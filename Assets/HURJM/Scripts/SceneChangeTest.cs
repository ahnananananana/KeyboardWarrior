using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class SceneChangeTest : Singleton<SceneChangeTest>
{
    /*private void Awake()
    {
        if(_instance == null)
            _instance = this;
        DontDestroyOnLoad(this);
    }
    // Update is called once per frame
    void Update()
    {
        if(Input.GetKeyDown(KeyCode.Space))
        {
            switch(GameData.current.m_CurScene)
            {
                case 0:
                    SceneLoader.current.SceneChange(1);
                    break;
                case 1:
                    SceneLoader.current.SceneChange(0);
                    break;
            }
        }
    }*/
}
